OUTPUT_ARCH(arm)

SECTIONS
{
    . = 0x02000000;

    . = 0x08000000;
    .text :
    ALIGN(4)
    {
        asm/crt0.o(.text);
        asm/code.o(.text);
        src/code.o(.text);
        asm/multiboot.o(.text);
        asm/code_2.o(.text);
        asm/libagbsyscall.o(.text);
        asm/code_3.o(.text);
        data/data.o(.rodata);
    } = 0

    /* Discard everything not specifically mentioned above. */
    /DISCARD/ :
    {
        *(*);
    }
}
